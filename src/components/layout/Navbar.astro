---

---

<nav class="w-full px-4 py-2">
  <ul class="flex flex-col items-start justify-start md:gap-2">
    <button
      class="w-full cursor-pointer text-start font-offBit text-sm tracking-widest text-whiteText hover:underline md:text-base"
      data-section="home"
    >
      Home
    </button>

    <button
      class="w-full cursor-pointer text-start font-offBit text-sm tracking-widest text-whiteText hover:underline md:text-base"
      data-section="project"
    >
      Projects
    </button>

    <button
      class="w-full cursor-pointer text-start font-offBit text-sm tracking-widest text-whiteText hover:underline md:text-base"
      data-section="blog"
    >
      Blogs
    </button>

    <button
      class="w-full cursor-pointer text-start font-offBit text-sm tracking-widest text-whiteText hover:underline md:text-base"
      data-section="about"
    >
      Contact
    </button>
  </ul>
</nav>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const buttons = document.querySelectorAll('button[data-section]');
    const sections = {
      home: document.getElementById('home-section'),
      project: document.getElementById('project-section'),
      blog: document.getElementById('blog-section'),
      about: document.getElementById('about-section'),
    };

    buttons.forEach((button) => {
      button.addEventListener('click', () => {
        const section = button.getAttribute('data-section');
        Object.keys(sections).forEach((key) => {
          if (key === section) {
            sections[key].style.display = 'block';
          } else {
            sections[key].style.display = 'none';
          }
        });
      });
    });

    Object.keys(sections).forEach((key) => {
      if (key === 'home') {
        sections[key].style.display = 'block';
      } else {
        sections[key].style.display = 'none';
      }
    });
  });
</script>
